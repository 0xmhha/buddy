---
description: actor → infra component 매핑 — actor × infra matrix + ownership + compliance scope.
argument-hint: "<project name>"
disable-model-invocation: true
---

# /buddy:map-use-cases-to-infra

actor system boundary → infra component 매핑 — Q8=(a) cascade 의 §2→§3 transition layer. 산출물은 actor × infra bidirectional matrix + cross-actor shared ownership + compliance scope.

## 실행 지시

`Skill` 도구로 `router` skill 을 호출하라. 다음 컨텍스트를 전달한다:

- mode: `single`
- target PROCEDURE: `map-use-cases-to-infra`
- 사용자 인자:
    ```
    $ARGUMENTS
    ```
