---
description: Infracost + unit economics ($/MAU) + waste detection + savings recommendation.
argument-hint: "<project name> v<version>"
disable-model-invocation: true
---

# /buddy:audit-cost-efficiency

Infracost monthly + per-component breakdown + unit economics ($/MAU) + waste detection + savings recommendation.

## 실행 지시

`Skill` 도구로 `router` skill 을 호출하라. 다음 컨텍스트를 전달한다:

- mode: `single`
- target PROCEDURE: `audit-cost-efficiency`
- 사용자 인자:
    ```
    $ARGUMENTS
    ```
