---
description: WCAG 2.2 AA 30+ criteria + a11y annotation 컨벤션 + component baseline + 색 대비 4.5:1 / focus 3:1 + keyboard / screen reader 검증 + ESLint jsx-a11y + axe-core CI.
argument-hint: "<제품 이름 또는 design system 경로>"
disable-model-invocation: true
---

# /buddy:design-accessibility-baseline

POUR 4 원칙 (Perceivable / Operable / Understandable / Robust) success criteria AA + component-level baseline. apply-design-system 정합 + audit-accessibility (§6) 입력.

## 실행 지시

`Skill` 도구로 `router` skill 을 호출하라. 다음 컨텍스트를 전달한다:

- mode: `single`
- target PROCEDURE: `design-accessibility-baseline`
- 사용자 인자:
    ```
    $ARGUMENTS
    ```
