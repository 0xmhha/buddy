---
description: 신규 release feature 의 awareness → trial → habit funnel + power user 5차원 코호트 + abandonment step 진단 + A/B test 후보.
argument-hint: "<feature 이름 또는 release 시점>"
disable-model-invocation: true
---

# /buddy:analyze-feature-adoption

3 stage funnel + power user (Hooked - Nir Eyal) + abandonment fix hypothesis. design-ab-experiment + generate-improvement-tasks 입력.

## 실행 지시

`Skill` 도구로 `router` skill 을 호출하라. 다음 컨텍스트를 전달한다:

- mode: `single`
- target PROCEDURE: `analyze-feature-adoption`
- 사용자 인자:
    ```
    $ARGUMENTS
    ```
