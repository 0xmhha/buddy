---
description: customer tier 별 segmentation + 자동 vs 수동 결정 + batch schedule (internal → friendly → 자동 → opt-in → 1:1) + rollback snapshot + communication 5 layer + telemetry 5 metric.
argument-hint: "<변경 대상 또는 segment>"
disable-model-invocation: true
---

# /buddy:migrate-customers

대규모 customer migration plan. deprecate-feature 의 대규모 사용자 영향 케이스 후속. Strangler Fig (Fowler) 패턴 + Stripe API migration playbook reference.
